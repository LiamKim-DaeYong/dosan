package com.samin.dosan.domain.training.training_target;

public class TrainingTargetService {
/*    Map<TargetKey,Integer> students = ObjectToMap.getDataToMap(Student);

    student
    save(TraingTarget.of(student.getKey(), student.getValue))*/
}
